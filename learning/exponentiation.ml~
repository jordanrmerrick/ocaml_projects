open Base
open Stdio

let rec sum ls =
  match ls with
  | [] -> 0
  | first :: rest -> first + sum rest

let rec exponent num exp =
  match exp with
  | None -> 0
  | Some x -> num *. exponent num exp-1

let () =
  printf "%i\n" (sum [1;2;3;4])

